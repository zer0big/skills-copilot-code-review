---
applyTo: "backend/**/*,*.py"
---

## 백엔드 가이드라인

- 모든 API 엔드포인트는 `routers` 폴더에 정의해야 합니다.
- 예제 데이터베이스 콘텐츠는 `database.py` 파일에서 불러옵니다.
- 오류 처리는 서버에만 로그로 남깁니다. 프론트엔드로 전파하지 마세요.
- 모든 API가 문서에 설명되어 있는지 확인합니다.
- 백엔드 변경 사항이 프론트엔드(`src/static/**`)에 반영되어 있는지 확인합니다. 잠재적인 호환성 문제를 발견하면 개발자에게 알려 주세요.